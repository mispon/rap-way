namespace UI.Controls.UnreadBadge
{
    public class EmailsUnreadBadge : BaseUnreadBadge
    {
        protected override void RegisterHandlers()
        {
            // todo:
            // inc on new email event
            // dec on open new email event
        }
    }
}